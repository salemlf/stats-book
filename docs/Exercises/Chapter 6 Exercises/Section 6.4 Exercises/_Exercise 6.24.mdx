In Exercise 6.4, we considered a random variable $Y$ that possessed an exponential distribution with mean $4$ and used the method of distribution functions to derive the density function for $U = 3Y + 1$.
Use the method of transformations to derive the density function for $U$.
